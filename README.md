# NodeJS Performance Demoes

## Preparations

Run `yarn` or `npm i` to install dependencies

## Profiling Example

1. Run `node --inspect index`
2. Open Chrome dedicated devtools for node
3. Profile the app
4. Try to optimize the function to run faster

## Garbage Collection

## Streams
